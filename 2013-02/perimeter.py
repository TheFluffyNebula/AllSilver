import sys
sys.stdin = open("perimeter.in")
# sys.stdout = open("perimeter.out", 'w')


'''
analyze

only the ones on the outside matter
outside: leftmost/rightmost in a row, topmost/botmost in a column
traversal could be tricky
'''
